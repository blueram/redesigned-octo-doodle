# MiniGame V4.5.3 TestBot Fortress Fix

- 테스트 봇 포트리스 지형 자동 선택
- 방장의 기기가 온라인 봇 차례를 대신 조준/발사
- playOrder 기준으로 탱크 순서 고정
- 봇 중복 발사 예약 방지

테스트 주소: `?test=1&v=453`

# MiniGame V4.5.0

- OX 혼자 모드: 최상 난이도, 1분 30초 생존
- OX 생존 최고 기록을 Firebase 서버에 사용자별 1개 저장
- 홈 화면과 모든 게임 대기실에 서버 상위 10위 표시
- firebase-rules.json의 oxSurvivalRank 규칙 적용 필요

# MiniGame V4.1.2

## 초성게임 모바일 입력 화면 개선
- 정답 입력창을 누르면 키보드 전용 압축 화면으로 자동 전환
- 상단 로고와 진행 보조정보를 임시 축소/숨김
- 문제, 힌트, 정답자, 실시간 순위, 입력창을 키보드 위 한 화면에 배치
- 입력 종료 시 원래 화면으로 자동 복귀
- iPhone Safari 및 PWA visualViewport 대응

# MiniGame V4.0 안정화 버전

## 포함 게임
- 초성게임
- OX 이동게임
- 포트리스

## V4.0 주요 개선
- 전적용 사용자 ID, 기기 ID, 접속 세션 ID 분리
- 같은 탭 새로고침 시 기존 세션 복구
- 새 탭/다른 기기는 별도 참가자로 처리
- Firebase Transaction으로 2명 정원 동시 입장 보호
- 10초 Heartbeat와 35초 비활성 세션 정리
- Firebase onDisconnect로 종료된 참가자 자동 제거
- 방장 접속이 사라지면 남은 참가자에게 방장 자동 승계
- OX 첫 판 선공 무작위, 다음 판은 직전 패자 선공
- 포트리스 첫 판 선공 무작위, 다음 판은 직전 패자 선공
- 포트리스 10가지 지형 썸네일 선택
- 온라인 포트리스는 두 사람 선택 중 하나를 무작위 적용
- 궤적 아이템 상태 및 가시성 개선
- PC/온라인 전적 분리

## 업로드
GitHub 저장소의 기존 파일을 이 ZIP의 파일로 모두 교체한 뒤 커밋하세요.
서비스워커 캐시 이름이 `minigame-v4-0`으로 변경되어 기존 캐시가 자동 교체됩니다.

## 온라인 테스트 권장
- 서로 다른 휴대전화 2대로 테스트
- 같은 휴대전화 테스트 시 새 탭은 별도 세션으로 처리
- 같은 탭 새로고침은 기존 참가자로 복구


## V4.1 초성퀴즈 개선
- 초성퀴즈 온라인 방 최대 4명 접속 (OX/포트리스는 기존 2명 유지)
- 2명 이상 및 참가자 준비 완료 시 방장 시작
- 입력창 바로 위에 1~4위 실시간 순위 고정 배치
- Firebase Transaction으로 동시 정답 중 최초 1명만 인정
- 정답자와 정답을 3초간 공개한 뒤 다음 문제 자동 진행
- 정답 공개 중 입력 잠금, 중간 퇴장 시 남은 참가자 순위 자동 재정렬
- 최종 결과에 전체 참가자 순위와 점수 표시

## V4.1.1 접속 오류 수정
- 초성퀴즈 참가자 입장 Transaction을 방 전체가 아닌 players 목록에만 적용
- heartbeat 갱신과 입장 처리 충돌 방지
- 실제 정원 초과와 네트워크 오류 메시지 분리
- 기존 생성 방에도 maxPlayers=4 자동 보정
- 서비스워커를 network-first로 변경하여 GitHub 업로드 후 구버전 캐시 방지


## V4.2.0
- 초성 입력 키보드가 열릴 때 1단계 힌트 표시가 상단으로 밀려 가려지는 현상 수정
- 힌트 단계와 내용이 한 영역에서 함께 유지되도록 모바일 입력 화면 조정


## V4.2.0
- 초성 입력창을 누르면 문제·힌트·실시간 순위 영역이 입력창 바로 위에 고정됩니다.
- 키보드가 열린 화면에서 초성 카드가 키보드 상단에 붙고, 카드 내부 입력창이 최하단에 배치됩니다.

## V4.5.0 초성퀴즈 자동 넘김
- 힌트 표시: 1단계 → 2단계 → 3단계(1/3) → 3단계(2/3) → 3단계(3/3)
- 세 번째 힌트가 3회 표시되면 시간 초과 처리
- 정답을 2초간 공개한 뒤 다음 문제로 자동 이동
- 혼자 하기와 온라인 대전에 모두 적용

## V4.5.2 테스트 봇 시작 수정
- 첫 문제에서 참가자 목록 조회보다 봇 자동 동작 예약이 먼저 실행되던 비동기 순서 오류 수정
- 매 라운드마다 Firebase의 최신 참가자 목록을 확인한 후 봇 동작 예약
- 라운드 토큰 검증으로 이전 문제의 예약 동작이 다음 문제에 반영되는 현상 방지


## V4.5.6
- 검토 완료용 200문제를 `chosung_questions.json` 별도 파일로 적용
- 초성게임 시작 전에 문제 파일 로드 완료 후 출제
- 힌트1·힌트2·힌트3을 순서대로 표시
- iPhone 키보드 표시 시 시각 뷰포트 상단에 게임 화면 고정
- 문서 전체 스크롤과 화면 튐 방지

## V4.5.6 포트리스 팀전
- 온라인 참가자 2명: 1:1
- 온라인 참가자 3명: 2:1
- 온라인 참가자 4명: 2:2
- 접속 순번 기준 A-B-A-B 자동 팀 편성
- 테스트봇과 실제 사용자를 섞어서 팀 구성 가능
- 각 봇의 명중 성향과 반응속도를 경기마다 무작위 배정
- 팀의 모든 탱크가 파괴되면 해당 라운드 종료
- 3라운드 선승 팀이 최종 승리
- 사망한 탱크는 턴에서 자동 제외
- 재경기는 현재 접속 중인 모든 참가자의 동의 기준
